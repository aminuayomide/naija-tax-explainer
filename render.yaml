services:
  - type: web
    name: naija-tax-explainer
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: NANSEN_API_KEY
        sync: false   # Render will prompt you to enter this value securely
